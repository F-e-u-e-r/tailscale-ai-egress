generated/*
!generated/.gitkeep

# Release artifacts built by scripts/package.sh
/dist/

*.backup.*
*.tmp
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/
.DS_Store

# Local, untracked working files. These must be ignored so scripts/package.sh --
# which stages `git ls-files --cached --others --exclude-standard` -- does not sweep
# them into the release archive.
/skills-staging/
/fable review.txt
